# Rise OAuth App Credentials
# Get these from the Rise Partnerships team
APP_ID=your_app_id_here
APP_SECRET=your_app_secret_here

# Redirect URL Configuration
# For local development: Use localhost URL here (e.g., http://localhost:3000/oauth/callback)
# When testing, you'll be redirected to your production URL first - copy that redirect URL,
# extract the instanceId parameter from it, and use that instanceId when making API calls locally.
REDIRECT_URL=http://localhost:3000/oauth/callback

# SDK Configuration
# Set to 'true' to use the Rise TypeScript SDK (recommended)
# Set to 'false' to use plain HTTP calls
USE_SDK=false

# Server Port
PORT=3000
